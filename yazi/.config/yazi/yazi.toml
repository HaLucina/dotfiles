[mgr]
# ディレクトリを先に、ファイルを後に表示
sort_dir_first = true
# 自然順ソート（file1, file2, file10 の順になる）
sort_by        = "natural"
# 大文字小文字を区別せずソート（お好みで。falseなら A, a, B, b の順）
sort_sensitive = false
# 逆順にする場合はここを true に
sort_reverse   = false

[manager]
show_hidden = true
